interface:
  display_name: "Dragon Writer"
  short_description: "Create or continue novels with durable outline, setting, chapter, and state files."
  default_prompt: "Use $dragon-writer to continue my existing novel from its outline, settings, and latest chapter state."
policy:
  allow_implicit_invocation: true
