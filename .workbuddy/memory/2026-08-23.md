# 2026-08-23

## Dashboard 响应式 + 右侧标题目录
- 修改 `dragon-writer/assets/dashboard.html`：
  - 自适应修复：`.layout` / `.graph-layout` / `.col2` 的 grid 列改为 `minmax(0,1fr)`，防止内容撑破；`main.content` 加 `min-width:0` 并 `margin:0 auto` 居中。
  - 新增右侧「本页目录」（`.page-toc`，≥1280px 显示，sticky）：由新 `PageToc` 模块从当前面板收集 h2 / .section-title / 设定文件卡片生成，支持点击平滑滚动（`scroll-margin-top:70px` 避开 appbar）、滚动跟随高亮（rAF 节流）、折叠设定卡片点击时自动展开；`Router.switchTab` 两条路径末尾调用 `PageToc.refresh()`。
- `build_dashboard.py` 构建校验通过，Node `--check` 语法通过。
- 同步范围（用户确认全部覆盖）：skill 安装副本 `~/.workbuddy/skills/d-writer__skillhub/assets/dashboard.html` + 4 本书项目（ashes-of-the-strongest、dushi-yaoyan-lu、sao-zhai-shou-nv、urban-harem-prince）。
- 注意：books 内副本若与模板不一致，多为旧世代残留而非定制；覆盖前先用 diff 确认。

## Dashboard 多主题 + 自定义背景
- 顶栏新增 🎨 外观面板（`Appearance` 模块），三段控制：
  - 模式：自动/浅色/深色分段控件（`Utils.theme` 新增 `set(mode)`，与 🌙 快捷按钮双向同步）。
  - 主题色：6 套 scheme（indigo 默认/ocean/forest/sunset/rose/violet），经 `data-scheme` 属性切换 `--accent`/`--accent-soft`，light/dark/auto 三模式各有配套色值；切换后 `Utils.theme.refresh()` + `GraphEngine.redraw()`。
  - 背景：`#bgLayer` 固定层（z-index 0，#landing/#app z-index 1），支持 无/点阵/网格/光晕/自定义图片；图片上传经 canvas 压缩（最长边 1600，JPEG 0.82）存 localStorage dataURL（CSP img-src 已含 data:），不透明度滑块 10–100%。
- 持久化 key：dw-scheme / dw-bg / dw-bg-image / dw-bg-opacity（模式仍用 dw-theme）。
- 样式打磨：appbar 毛玻璃（color-mix + backdrop-filter，@supports 兜底）、body 背景色过渡。
- 验证：build 校验 + node --check + agent-browser 冒烟（scheme×dark 正交、滑块、Esc 关闭、清除图片、刷新恢复）全通过；已同步 skill 与 4 本书。
- agent-browser 已全局安装（Chrome 152 在 ~/.agent-browser/browsers/），后续 UI 改动可复用此冒烟流程。
