# 2026-08-09

## 外层 README + 截图
- 在仓库根新建 README.md（项目总览、目录结构、仪表盘 4 标签页说明、工作流模式表、快速开始、开发测试命令），截图存根目录 screenshots/（landing/overview/settings-content/chars/chars-graph/read/dark 共 7 张，来自 Edge headless 走查）。
- 同步内层 dragon-writer/README.md 过时的仪表盘描述：5 标签页 → 4（删设定完成度），人物关系段落更新为缩放/平移/碰撞检测/主配徽章等新能力。

## dashboard 第三轮迭代（删设定完成度 + 关系图细节修复）
- 删除「设定完成度」标签页：移除 tab 按钮、panel、Settings 组件与 .domain CSS；快捷键 Ctrl+1~5 重排为 Ctrl+1~4；computeSettingCompletion 保留（设定内容页仍在用，卡片右上角显示完成度%）。
- 边标签防覆盖：draw() 内维护 placedLabels 矩形数组，新标签与已放置标签碰撞则跳过；自动显示阈值从 scale>=0.75 提到 0.85。
- 关系图左右栏等高：.graph-layout 改固定高度行（height:420px;max-height:70vh），画布 height:100%，侧栏 stretch + min-height:0 + 内部滚动；移动端媒体查询改回 height:auto。
- 角色列表区分主配：charcard 加 major/minor 类——主要角色左侧靛蓝色条 + 右上角「主要」徽章（accent-soft 底），次要角色灰边 + 灰徽章；tier 行只显示故事功能。
- 验证：构建+语法通过；截图确认 4 标签导航、侧栏等高、徽章、标签防覆盖。

## dashboard 整体样式重构 + 关系图大升级（第二轮）
- 用户选定「现代极简（浅色）」方向：全套 CSS 变量换成冷灰中性色 + 靛蓝 accent（#4f46e5，深色模式 #818cf8），UI 标题从衬线改无衬线（阅读区正文保留衬线），按钮改为白底细边框 Linear 风格。
- GraphEngine 重写为「世界坐标系 + 视口变换」：滚轮以光标为中心缩放（0.25–3x）、拖拽空白平移、双击适应画布、右上角工具条（＋/－/⛶/1:1）、左下角操作提示。
- 可读性：二次曲线连线（按边确定性弯曲避免平行边重叠）、边标签药丸底+描边、缩放 <0.75 隐藏边标签、<0.55 隐藏次要角色名、悬停/选中高亮邻居（其余淡出）、主要角色内圈/次要外圈的环形初始布局、预计算度数/半径/邻接表（原来每帧每节点 edges.filter）。
- Utils.theme 缓存补 bgElevated（边标签药丸底色，深色模式适配）。
- 验证：22 角色压力测试页（合成数据）确认布局与连线清晰；全页面 + 深色模式截图走查通过。
- 经验：Edge headless `--virtual-time-budget` 下 CSS animation 可能停在中间帧（页面发白假象），测试页注入 `*{animation:none!important}` 规避；headless 截图命令偶发静默失败，重试即可；Edge 截图 URL 须用正斜杠字面量，变量拼接易被 MSYS 转换搞坏。

## dashboard.html 视觉与体验优化
- 补齐了大量缺失的组件 CSS：设定完成度（.domain/.row/.chev/.progress-bar 折叠交互此前完全无样式、失效）、设定内容（.settingfile）、阅读器（.reader/.toc/.readnav/.read-actions/.search-bar/mark 高亮）、角色列表网格、滚动条。
- 修复总览进度圆环：conic-gradient 中 `--p` 无单位导致渐变无效（圆环不显示）；改为 `--p:42%` + `@property` 注册，渲染时从 0 过渡到目标值。
- 视觉打磨：按钮 disabled/按压态、标签页选中左侧指示条、面板切换淡入动画、统计卡悬浮、落地页 hero（径向光晕+浮动 logo）、章节标题装饰线、关系图画布点阵背景、审计漂移按类型着色（已修复绿/已知漂移黄）、诊断「无异常」补闭合标签。
- 响应式补充：≤1024px 阅读器收窄、≤768px 目录取消 sticky、搜索栏换行。
- build_dashboard.py 构建戳改为幂等（先清旧戳再写入），清理了 dashboard.html 中堆叠的 4 条旧戳。
- 验证：build 全检查通过；node --check 语法通过；Edge headless 截图走查 5 个标签页 + 落地页 + 深色模式（harness 预注入 tests/fixtures/standard-book 数据，生成脚本在 %TEMP%/gen_dash_harness.py）。
